---
import Layout from '../../layouts/Layout.astro';
import { t, getLocalePath } from '../../i18n/translations';

const lang = 'zh';
---

<Layout title="首页" lang={lang}>
  <section class="hero">
    <div class="hero-inner container-wide">
      <div class="hero-text fade-in">
        <p class="hero-eyebrow">建造者。创造者。进行中。</p>
        <h1 class="hero-greeting">{t(lang, 'home.greeting')}</h1>
        <p class="hero-tagline">{t(lang, 'home.tagline')}</p>
        <p class="hero-tagline-alt">{t(lang, 'home.tagline2')}</p>
        <p class="hero-subtext">{t(lang, 'home.subtext')}</p>
        <div class="hero-ctas">
          <a href={getLocalePath(lang, '/journey')} class="cta cta-primary">{t(lang, 'home.cta.journey')}</a>
          <a href={getLocalePath(lang, '/now')} class="cta cta-secondary">{t(lang, 'home.cta.now')}</a>
          <a href={getLocalePath(lang, '/writing')} class="cta cta-secondary">{t(lang, 'home.cta.writing')}</a>
        </div>
      </div>
      <div class="hero-image fade-in">
        <div class="hero-image-frame">
          <img src="/images/homepagepic.jpg" alt="Tom Wang" />
        </div>
      </div>
    </div>
  </section>

  <section class="section quote-section">
    <div class="container">
      <div class="home-identity fade-in">
        <blockquote class="identity-quote">
          "我是一个完成使命的建造者 —— 被好奇心驱动，相信建造是改变世界的方式。"
        </blockquote>
      </div>
    </div>
  </section>
</Layout>

<style>
  .hero {
    background: var(--color-hero-gradient-start);
    color: var(--color-hero-text);
    padding: 0;
    min-height: 85vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: -50%;
    right: -20%;
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(180, 83, 9, 0.15) 0%, transparent 70%);
    pointer-events: none;
  }

  .hero-inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    padding-top: 4rem;
    padding-bottom: 4rem;
  }

  .hero-text {
    max-width: 540px;
  }

  .hero-eyebrow {
    font-size: 0.8rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--color-accent);
    margin-bottom: 1.5rem;
  }

  .hero-greeting {
    font-family: 'Playfair Display', var(--font-serif);
    font-size: 4rem;
    font-weight: 700;
    letter-spacing: -0.04em;
    margin-bottom: 1.5rem;
    line-height: 1.1;
    color: var(--color-hero-text);
  }

  .hero-tagline {
    font-size: 1.2rem;
    line-height: 1.65;
    color: rgba(250, 250, 249, 0.85);
    margin-bottom: 0.25rem;
    font-weight: 400;
  }

  .hero-tagline-alt {
    font-size: 1.2rem;
    line-height: 1.65;
    color: rgba(250, 250, 249, 0.85);
    margin-bottom: 1.5rem;
    font-weight: 400;
  }

  .hero-subtext {
    color: var(--color-hero-muted);
    font-size: 0.95rem;
    margin-bottom: 2.5rem;
    font-style: italic;
  }

  .hero-ctas {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
  }

  .cta {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border-radius: 9999px;
    font-size: 0.875rem;
    font-weight: 600;
    text-decoration: none;
    transition: all var(--transition);
    letter-spacing: 0.01em;
  }

  .cta-primary {
    background: var(--color-accent);
    color: white;
  }

  .cta-primary:hover {
    background: var(--color-accent-hover);
    color: white;
    transform: translateY(-1px);
  }

  .cta-secondary {
    background: transparent;
    color: var(--color-hero-muted);
    border: 1px solid rgba(168, 162, 158, 0.3);
  }

  .cta-secondary:hover {
    color: white;
    border-color: rgba(255, 255, 255, 0.5);
  }

  .hero-image {
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .hero-image-frame {
    width: 380px;
    height: 480px;
    border-radius: var(--radius-xl);
    overflow: hidden;
    position: relative;
    box-shadow: 0 32px 64px rgba(0, 0, 0, 0.4);
  }

  .hero-image-frame img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
  }

  .quote-section {
    position: relative;
  }

  .home-identity {
    padding: 2rem 0;
  }

  .identity-quote {
    font-family: 'Playfair Display', var(--font-serif);
    font-size: 1.5rem;
    line-height: 1.7;
    color: var(--color-text-muted);
    border-left: 3px solid var(--color-accent);
    padding-left: 2rem;
    font-style: italic;
    max-width: 620px;
  }

  @media (max-width: 968px) {
    .hero-inner {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 3rem;
    }

    .hero-text {
      max-width: 100%;
      order: 2;
    }

    .hero-image {
      order: 1;
    }

    .hero-image-frame {
      width: 280px;
      height: 350px;
    }

    .hero-ctas {
      justify-content: center;
    }

    .hero-greeting {
      font-size: 3rem;
    }
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
    }

    .hero-inner {
      padding-top: 3rem;
      padding-bottom: 3rem;
    }

    .hero-greeting {
      font-size: 2.5rem;
    }

    .hero-tagline, .hero-tagline-alt {
      font-size: 1.05rem;
    }

    .hero-ctas {
      flex-direction: column;
      align-items: center;
    }

    .cta {
      width: 100%;
      max-width: 280px;
      text-align: center;
    }
  }
</style>
